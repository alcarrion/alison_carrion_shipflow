package com.pucetec.alison_carrion_shipflow.routes

class Routes {
    companion object {
        const val BASE_PATH = "/api/shipflow"
        const val SHIPMENTS = "$BASE_PATH/shipments"
        const val STATUS = "/status"
        const val EVENTS = "/events"
    }
}
